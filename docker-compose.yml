version: '3.8'

services:
  frontend:
    build:
      context: .
      target: frontend
    ports:
      - "8508:8508"
    environment:
      - NODE_ENV=production
    restart: always

  backend:
    build:
      context: .
      target: backend
    ports:
      - "8509:8509"
    environment:
      - NODE_ENV=production
    restart: always